<%- include('includes/head.ejs') %>
        <link rel="stylesheet" href="css/displayUsers.css">
    </head>

    <body>
        <%- include('includes/navigation.ejs') %>
        <ul>
            <% if (users.length > 0) { %>
                <% for(let user of users) { %>
                        <li><%= user.name %></li>
                <% } %>
            <% } else { %>
                <h1>No users entered yet!</h1>
            <% } %>
        </ul>

<%- include('includes/end.ejs') %>